---
author: GLM-5.3 (智谱 Z.ai)
tags: [LLM, 速通, Transformer, 大语言模型, RLHF]
created: 2026-08-27
updated: 2026-08-27
source: 由 GLM-5.3(智谱 Z.ai)生成·零基础上手系列
---

# 第四章 · 大语言模型(LLM)

> 本章地图:LLM 的本质(文字接龙机器?) → Tokenizer 怎么切词 → Decoder-only 架构逐层拆解(Embedding/Attention/FFN/LayerNorm/位置编码) → 三大模型范式之争 → 预训练与缩放定律 → SFT 与 RLHF/DPO/GRPO 对齐三部曲 → 推理模型与测试时计算 → 代表模型谱系 → 部署推理优化(KV Cache/量化/LoRA) → 幻觉机制与提示工程速成。
> 🔗 系列:[[00-课程总览]] | 上一章:[[03-自然语言处理]] | 下一章:[[05-计算机视觉]]
> ⭐⭐⭐ 全书最重要一章。读不懂 Attention 就再读一遍 4.3,这是这一行的"乘法口诀"。

## 4.1 神迹还是套路?LLM 的本质一句话

**大语言模型 = 在超大规模语料上训练的"下一个词预测器"(next-token predictor)。**

给它"床前明月",它算出下一个 token 是"光"的概率 87%、"下"6%……采样一个输出,拼回去继续预测——如此循环,就是 ChatGPT 回答你的全部机制。

💡 三个常被追问的问题:

| 质疑 | 回应 |
|---|---|
| "不就是概率接龙?" | 要把接龙做好,**必须**内隐地学会语法、事实知识、推理模式——压缩即智能(压得好=理解得深)。接龙是任务形式,不是能力上限 |
| "它真的懂吗?" | 取决于"懂"的定义。它有完整的世界统计模型却没有感知/意图,更像"冻结在权重里的人类文字文明模拟器" |
| "为什么这么简单的事能造成轰动?" | 因为规模。当参数×10³、数据×10³,"量变引发质变"——下一节开始拆 |

## 4.2 Tokenizer:先解决"文字怎么进计算机"

### BPE 的完整直觉

神经网络吃定长数字向量,所以先把字符串切成 **token**,每个 token 查表变向量。

**BPE(Byte Pair Encoding)训练过程(以玩具语料为例)**:

```
初始:把所有词拆成最小单位        l o w / l o w e r / n e w e s t ...
第1轮:统计最高频相邻对 "e"+"s"   合并 → es
第2轮:"es"+"t"                  合并 → est
第3轮:"l"+"o"                   合并 → lo ...
…重复直到达到目标词表大小(如 15 万)
```

💡 原则:**高频组合合并成一个 token,低频保持碎片**。"the" 一个 token;"antidisestablishmentarianism" 拆七八片。好处:任何新词都能兜住(退化到字符),常见词又省上下文长度。

⚠️ 实用冷知识:
- 中文常用字多为 1 字=1 token,英文一般 ~0.75 词/token;
- **数字被切碎后运算容易错**(token 边界破坏数位结构)、代码缩进/生僻 Unicode 同理;
- 价格、上下文窗口、生成速度全都按 token 计费——**token 是 LLM 世界的通用货币单位**。

### 从 token 到向量与对话模板

```
文本 → tokenize → token id 序列[15496, 11...] 
     → Embedding 查表(15万行 × 4096维的大矩阵)
     → 加上位置信息 → 进入 Transformer 主干 ×N 层
     → 最后 LayerNorm → LM Head(只取最后一个位置的向量)
     → 映射回 15 万维 logits → softmax → 概率分布 → 采样
```

⚠️ **对话聊天必须套 chat template**:系统提示+历史各轮会被渲染成一串特殊 token(`<|im_start|>user`…)包好再送进去。模型眼里没有"消息气泡",只有一条长长的 token 流——**一切 UI 都是幻觉,一切幕后都是 token**。

## 4.3 Transformer 主干逐层拆解(全书核心技术段落)

现代 LLM 几乎清一色 **Decoder-only**。一层积木长这样:

```
x ─┬─→ [多头自注意力 MHA] ──┐
   │                       ⊕ ←──残差连接
   ├───────────────────────┘
   ↓ (RMSNorm)
x'─┬─→ [前馈网络 FFN] ──────┐
   │                       ⊕ ←──残差连接
   └───────────────────────┘
   ↓ 输出给下一层 …(共 N 层,GPT-3 是 96 层)
```

(Pre-Norm 结构:先归一化再进子层;残差小路直通底部)

### ① 自注意力 Self-Attention(重点中的重点)

对每个输入向量 xᵢ,三个线性变换造出三种角色:

$$Q = XW^Q,\quad K = XW^K,\quad V = XW^V$$

然后一步核心公式:

$$\text{Attention}(Q,K,V) = \text{softmax}\left(\frac{QK^\top}{\sqrt{d_k}}\right)V$$

三个角色就是第三章开头 Attention 思想的矩阵化落地:

| 角色 | 直觉职责 | 💡图书馆类比 |
|---|---|---|
| **Q 查询 Query** | 我现在要找什么信息?(带着这个问题去检索) | 你喊出的检索关键词 |
| **K 键 Key** | 我这里有什么类型的资讯?(供匹配的名片) | 书脊上的索书号 |
| **V 值 Value** | 匹配上了就实际带走的内容本身 | 书里的正文 |

计算流:每行的 Q 和所有 K 点积打分(**谁和我相关**)→ 除以 √dₖ 再 softmax(变成总和 1 的注意力权重)→ 按权重加权求和 V(**按比例混合大家的内容**)。

两个必考细节:
- **为什么要除 √dₖ?** 维度越大点积方差越大(dₖ=128 时分数动辄几十上百),softmax 会饱和成 one-hot,梯度死掉。除 √dₖ 把分数拉回合理区间,让注意力"软"着。
- **因果遮罩 Causal Mask**:解码时禁止偷看未来——QKᵀ 打分后,把每个位置对"未来位置"的分数置 -∞,softmax 后为零。这就是"Decoder"的全部含义:**永远只能左看右,不能右看左**。(训练时一次并行处理整句就靠这个 mask 实现"合法地同时学所有位置的下一步预测"。)

💡 直觉总结一句:**Attention = 可微分的软字典查询**——普通 dict 是硬命中一个 key,这里是全体 key 都命中一点、按相关度加权取 value,而且整个过程可求导、能端到端学习。

### ② 多头 Multi-Head:开十个视角的分会场

单头注意力只有一个"关注模式"。把 Q/K/V 分成 h 组(如 96 头)各自独立做注意,结果拼接——有的头盯语法结构、有的头追指代链、有的头记长程事实。**多头 = 让不同的头专业分工**。工程演化(了解即可):MQA/GQA 让多个 Q 头共享一组 KV,大幅压缩推理时 KV 缓存显存(现代 LLM 标配);DeepSeek 的 MLA 更进一步把 KV 压进低秩潜空间。

### ③ FFN 前馈网络:模型的"知识仓库"

```
FFN(x) = act(x·W₁)·W₂      (中间维度一般是隐层的 4 倍左右)
```

参数大头在这里(约 2/3)。研究共识:注意力负责"信息搬运与路由",**FFN 以键值对形式存储大量世界知识**("巴黎→法国首都")。MoE(Mixture of Experts)就是把单个巨大 FFN 切成多位专家,路由器按 token 选择性激活少数几个——**用稀疏激活换容量**:总参数几万亿,每次推理只用百分之几,Qwen/DeepSeek 旗舰都是 MoE。

### ④ 位置编码:序列的"第几排"

注意力本身是集合运算(打乱顺序结果不变!),但语言顺序敏感("我打你"≠"你打我"),必须注入位置信息:

| 方案 | 做法 | 状态 |
|---|---|---|
| 正弦编码 | 不同频率的正余弦叠加 | 元老 |
| 可学习绝对位置 | 每个位置一张可训向量 | GPT-2 时代 |
| **RoPE 旋转位置编码** | 用旋转矩阵把 Q/K 旋转"角度∝相对距离",使 attention score 只依赖相对位置 | **当今主流** |
| 外推技巧 YaRN 等 | 微调少量参数把短窗模型拉长到长窗 | 长上下文标配 |

为什么主流程逝了绝对改推 RoPE?**外推友好**——训练到 8K、想推理 128K,只需微调旋转频率而非重新训练。

## 4.4 三大范式:BERT / GPT / T5 与 Decoder-only 的完胜

| | Encoder-only(BERT) | Decoder-only(GPT) | Encoder-Decoder(T5/BART) |
|---|---|---|---|
| 训练目标 | 完形填空(挖空猜词,可双向看) | 下一个词预测(带因果 mask) | 读入原文生成目标文 |
| 强项 | 理解类任务分类/NER | **开放生成** | 成对的转换翻译摘要 |
| 一句话结局 | 被 LLM 直接替换 | 统一天下 | 留存于特定翻译场景 |

**Decoder-only 为什么赢?**(面试高频,四条)
1. **目标统一**:预训练目标(next-token)与使用方式(生成)完全一致,SFT/RLHF 都顺滑衔接;
2. **Scaling 干净**:无需区分 enc/dec 结构,堆层数最简单,缩放定律直接适用;
3. **KV Cache 天然友好**:单向注意力意味着历史部分的 KV 可以缓存复用(见 4.7);
4. **涌现能力发源地**:GPT-3 已证明 in-context learning,生态全押这边,飞轮转起来了。

## 4.5 预训练 Pre-training:文明的一面镜子

- **数据构成**(约万亿~百万亿 token):网页文本(最大头) + 代码(极重要!显著提升逻辑推理) + 书籍(长程连贯性) + 论文/百科(知识密度)。经抽取、去重、质量过滤、安全清洗四道工序;**基准测试污染检测**是当代必修课。
- **训练目标**:全部 token 上做交叉熵(第二章的点在这里闭环:**LLM 预训练 = 一道做了几万亿次、15 万选一的选择题**)。
- **缩放定律 Scaling Laws**(OpenAI/Chinchilla 两版):损失随参数、数据、算力呈幂律平滑下降,并可提前用小模型外推大模型性能——**这是各家敢一笔投下亿美元训练的数学信心来源**。Chinchilla 定律给出指导:给定算力预算,参数与数据应大致同比扩张(约为每参数 20 token)。
- **涌现 Emergence**:某些能力(多步算术、指令遵循、翻译)在小模型几乎为零,过了某个规模线突然出现。⚠️ 学界争议:可能是度量指标造成的"跳变假象",也可能是真实相变——不管哪种,实践上"更大确实更强"至今未破产。

## 4.6 后训练三部曲:从"续写机"到"助手"(重中之重)

预训练出来的裸模型只会**续写**——你说"怎么煮鸡蛋?"它可能续写出一整页《鸡蛋养殖技术问答汇编》(因为它在模仿网页文体!)。变成听人话的助手,还差三步:

### 第一步 SFT(Supervised Fine-Tuning 监督微调)

人工/AI 编写高质量"指令→回答"对(十万级量级即可,因为只是教会**格式与行为**,非灌输知识),交叉熵训练——教会模型"用户是在问它问题,期待一个直接的帮助性回答"。对话模板、多轮格式在这一步定型。两阶段是标准形态:通用指令泛化 + 场景垂直专精(可提一句[[S3训练营 第四周-FunctionCall微调项目|FC 场景实例]])。

### 第二步 RLHF(Reinforcement Learning from Human Feedback)

SFT 只是模仿示范答案,**不知道人类更偏好哪种风格**。RLHF 三步走:
1. 采集人类偏好对比("回答 A 比回答 B 好",几万对);
2. 训练奖励模型 RM 给任意回答打分;
3. PPO 强化学习让策略模型最大化 RM 打分(加 KL 惩罚拴住不让飘太远)。

成果是质变的:有用、诚实、无害的风格主要来自这步(InstructGPT 论文核心结论)。代价:RM 可能被钻空子(reward hacking,如学会讨好冗长)+ 流程复杂。
**简化替代 DPO**:跳过 RM 和在线 RL,直接用偏好对做离线优化——简单稳定,成为开源社区主流;**GRPO** 则去掉价值网络按组内对比算优势,因 DeepSeek-R1 出圈成为 Agent/推理时代的热门配方(三者对照:[[S2训练营 RL-PPO概念图解|PPO 细节]]/[DPO]/[GRPO],此处只需要记住:**PPO=在线+RM,DPO=离线偏好对,GRPO=组内相对比较省 critic**)。

### 第三步 RLVR:推理模型的诞生(2024→ 至今最前沿)

传统 RLHF 依赖主观偏好;**RLVR(RL with Verifiable Reward)用程序化验证器代替 RM**:数学题核对最终答案、代码跑单元测试——奖励客观不可作弊。代表成果 o1/o3、DeepSeek-R1:**不用人工示范,纯靠大规模强化学习自发涌现出长思维链**(自查、回溯、"等等我再想想")。标志范式的关键词是:

> **Test-time Compute Scaling 测试时计算扩展**——不仅"训练越久越聪明",还可以"**思考越久越准**":同一道难题,允许模型生成 10 倍长的思考链或多路采样择优,准确率显著上升。这打开了继预训练、后训练之后的第三条规模化曲线。

## 4.7 部署与推理:从权重到服务(工程师视角)

### KV Cache:推理加速的头号功臣

自回归生成每出一个字都要"重看全文"?没必要——前面 token 的 K/V 根本没变。**把它们的 K/V 缓存起来**,新 token 只需算自己的 q 并与缓存点积。代价:显存占用 = 2×层数×头维度×上下文长度×批大小(128K 上下文时代极其可观),于是有了 GQA/MLA 压缩、PagedAttention 分页管理(vLLM 核心思想)。

### 采样策略:多样性与稳态的旋钮

| 参数 | 作用 | 建议 |
|---|---|---|
| Temperature | logits 温度,<1 收敛保守,>1 天马行空 | 严谨任务 0~0.3,创意 0.7~1.0 |
| Top-p (nucleus) | 只在累计概率 p 内的候选中采样 | 通用 0.9 |
| Top-k | 只保留最高 k 个候选 | 与 top-p 二选一 |

重复惩罚/frequency penalty:抑制复读机行为。

### 其他关键技术速查

| 技术 | 一句话 | 效果 |
|---|---|---|
| **量化 Quantization** | FP16→INT8/INT4(AWQ/GPTQ/BNB) | 显存÷2~4,小损质量 |
| FlashAttention | 分块计算避免物化 n×n 注意力矩阵 | 快+省显存,训练推理通吃 |
| 投机解码 Speculative | 小草稿模型快速起草,大模型批量验证 | 2~3 倍无损加速 |
| Continuous Batching | 动态插入新请求充分利用 GPU | 服务吞吐数倍提升 |
| **LoRA/QLoRA** | 冻结原权重,旁路挂低秩小矩阵训练 | 千卡级参数微调→单卡可玩 |

## 4.8 幻觉 Hallucination:机制的清醒认识

幻觉 = **输出流畅、自信、具体,但事实上错误或凭空捏造**。

**为什么会发生?(结构性原因)**
1. 目标函数奖励"合理的下一个词",不奖励"真实的下一个词";
2. 知识以统计规律形式分布式存储,边界模糊处必然插值捏合;
3. 训练数据本身有错/过期;
4. 缺乏"我不知道"的有效内省信号(校准欠佳);
5. 用户提问预设陷阱("鲁迅和周树人见过面吗?")直接掉坑。

**缓解工具箱**(分层使用):
- 检索增强 RAG:引用外部资料作答并注明出处(工程主流,详见 S2/S4);
- 允许拒答:SFT 中加入"承认不知道"的示范样本;
- 思维链自查 + 自一致性(多次采样投票);
- 工具调用外部验证(计算器/代码执行);
- 后审校:LlamaGuard 类检查器二次过筛。

💡 一句话记住:**幻觉不是 bug 而是 next-token 范式的影子**——压制幻觉=约束生成自由度,体验与可靠性的平衡点是产品设计的核心命题。

## 4.9 提示工程速成(先把模型用对,再谈微调)

六板斧按性价比排序:
1. **明确角色+任务+约束**:角色限定风格,任务定义产出,约束框死范围("只输出 JSON,不要解释");
2. **Few-shot 示例**:2~5 个样例胜过千言描述;
3. **思维链引导**:"请一步步思考"在复杂题上是免费的午餐;
4. **结构化输出**:XML/JSON 包裹关键段;新趋势是大模型厂商内置结构化输出接口;
5. **迭代打磨**:把失败案例喂回去改进提示(badcase 驱动,S3 营九原则同源);
6. **防御性设计**:对注入攻击(提示夹带恶意指令)保持警惕——长会话的安全性从来不是白来的。

## 4.10 评估与代表谱系(点到为止)

**公开基准**:MMLU(学科知识)/GPQA(研究生级科学)/HumanEval & LiveCodeBench(代码)/MATH-AIME(数学)/Arena Elo(人类盲测偏好)。⚠️ 记住两件事:①刷分 ≠ 能力,榜单有污染与过拟合风险;②中文场景务必看 C-Eval/CMMLU 类中文榜。

**闭源头部**:GPT/Claude/Gemini——前沿能力通常最强,贵且黑盒。
**开源中坚**:Llama( Meta )、Qwen(阿里,全球下载前列)、DeepSeek(性价比与推理模型出圈)、Mistral/GLama 家族——可私有部署、可微调,企业生产的主力军。
选择建议原则:**能开源不开源(可控+便宜),够用就好;前沿任务看能力天花板,业务任务看成本与合规。**

## ❓自测十二问

1. LLM 本质是一台什么机器?四个质疑点的回应是什么?
2. BPE 是怎么炼出来的?"the"和生僻长词的 token 待遇差异说明什么?
3. 把 Q/K/V 分别对应到图书馆三要素;"可微分软字典"指什么?
4. √dₖ 和 causal mask 各防的是什么?
5. 多头为了什么?GQA 解决哪个瓶颈?
6. FFN 在模型里扮演什么角色?MoE 如何用它换容量?
7. 注意力本身是集合运算的后果是什么?RoPE 怎么救?
8. Decoder-only 四条取胜理由?
9. Chinchilla 定律说了什么关系?
10. SFT/RLHF/RLVR 三步各自补足什么?DPO 与 GRPO 相对 PPO 简化了什么?
11. KV Cache 为什么有效?它的显存开销随哪几个量增长?
12. 幻觉的三条结构性成因?五招缓解?

## 🧭 下一站

文字王国通关。接下来换感官:[[05-计算机视觉]]——卷积核如何看世界,扩散模型如何凭空作画。
