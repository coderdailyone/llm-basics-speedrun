# AI 分层模拟卷（二）· 自然语言处理方向

> 结构与《AI分层模拟卷》原卷一致：基础题 3 道（各 10 分钟）、前沿题 3 道（各 15 分钟）、编程题 2 道（改错 20 分钟 + 构建 25 分钟）。

---

## 第 1 题（基础题）

**题目描述**：
把词交给计算机处理，最朴素的方法是独热编码（one-hot），更好的方法是词向量（词嵌入）。请说明独热编码的缺陷，并解释词向量为什么能表达"猫和狗比猫和汽车更相似"这类语义关系。

**建议从以下角度组织答案**：
1、说明独热编码下任意两个词的向量关系如何，语义信息去了哪里；
2、说明词向量是如何训练出来的（提示：一个词的意义由它的上下文决定）；
3、举例说明词向量空间中"距离"或"方向"能表达什么。

**答题要求**：
请在下方答题区作答，建议作答字数 200~300 字，建议作答时长 10 分钟。

---

## 第 2 题（基础题）

**题目描述**：
什么是语言模型？请说明它的核心任务，并对比早期的 N-gram 统计方法与神经网络语言模型相比各自的特点与局限。

**建议从以下角度组织答案**：
1、用一句话定义语言模型的核心任务（给定前文预测什么）；
2、说明 N-gram 的做法及其三个主要缺陷（数据稀疏、无泛化、窗口短，任答其二即可）；
3、说明神经网络语言模型如何缓解这些缺陷。

**答题要求**：
请在下方答题区作答，建议作答字数 200~300 字，建议作答时长 10 分钟。

---

## 第 3 题（基础题）

**题目描述**：
大语言模型的输入输出以"token（词元）"为单位计量。请解释什么是 token、为什么现代模型不直接按"完整的词"或"单个字符"处理文本，并举一个 token 化带来的模型"怪癖"。

**建议从以下角度组织答案**：
1、说明 token 是介于字符和词之间的切分单位，常见词与生僻词分别如何被切分；
2、分析按完整词与按字符处理各自的问题（词表过大/生词、序列过长）；
3、举例说明 token 化的副作用（如数不清单词里的字母、按 token 计费）。

**答题要求**：
请在下方答题区作答，建议作答字数 200~300 字，建议作答时长 10 分钟。

---

## 第 4 题（前沿题）

**题目描述**：
2018 年出现的 BERT 与 GPT 代表了预训练语言模型的两条路线：完形填空式的双向理解与预测下一词的单向生成。请说明两者训练目标的区别，并分析为什么最终是生成式路线通向了今天的大语言模型。

**建议从以下角度组织答案**：
1、分别说明 BERT 和 GPT 在预训练时让模型做什么任务；
2、说明两者各自天然擅长的任务类型；
3、从"生成能否涵盖理解""任务的统一性"等角度，分析生成路线胜出的原因。

**答题要求**：
请在下方答题区作答，建议作答字数 200~300 字，建议作答时长 15 分钟。

---

## 第 5 题（前沿题）

**题目描述**：
GPT-3 展示了"上下文学习"（in-context learning）能力：不更新任何模型参数，只在提示词里给几个示例，模型就能完成新任务。请解释这一现象，并说明它为什么改变了人们使用 NLP 技术的方式。

**建议从以下角度组织答案**：
1、描述少样本提示的具体做法（给出示例格式）；
2、强调"没有重新训练、没有修改参数"这一关键点，说明它与微调的区别；
3、分析它如何把 NLP 的使用门槛从"训练模型"降到"写提示词"，以及这对普通用户意味着什么。

**答题要求**：
请在下方答题区作答，建议作答字数 200~300 字，建议作答时长 15 分钟。

---

## 第 6 题（前沿题）

**题目描述**：
"思维链"（Chain-of-Thought）是指让模型在给出最终答案前先写出推理步骤。实验表明这能大幅提升数学和逻辑题的正确率。请解释思维链为什么有效，并说明它与"推理模型"的关系。

**建议从以下角度组织答案**：
1、说明模型直接给答案相当于"心算"，其单步计算量是有限的；
2、解释把中间步骤写出来相当于什么（提示：把计算展开到输出里、每步以前面的结果为条件）；
3、简述后来的"推理模型"如何把这一思想推进了一步（自动生成很长的思考过程再作答）。

**答题要求**：
请在下方答题区作答，建议作答字数 200~300 字，建议作答时长 15 分钟。

---

## 第 7 题（编程题·改错题）

**题目描述**：
某同学做一个垃圾短信分类实验：先把所有文本转成 TF-IDF 特征，再划分训练集和测试集，最后训练并评估。代码如下：

```python
from sklearn.feature_extraction.text import TfidfVectorizer
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LogisticRegression

vectorizer = TfidfVectorizer()
X_all = vectorizer.fit_transform(texts)      # 在全部数据上统计词表和词频

X_train, X_test, y_train, y_test = train_test_split(
    X_all, labels, test_size=0.2, random_state=42)

clf = LogisticRegression()
clf.fit(X_train, y_train)
print("测试集准确率:", clf.score(X_test, y_test))
```

（注：`fit_transform` 会先从数据中**统计词表与文档频率**（fit），再据此转换数据（transform）；`transform` 只做转换，不再统计。）

**请回答以下问题**：
(1) 上述实验流程存在什么问题？
建议从以下角度组织答案：分析特征统计这一步"看到"了哪些数据；
(2) 解释为什么这个问题会让测试集准确率虚高，不能反映模型面对真实新数据的水平。
(3) 请修改代码，使实验流程正确。（先划分、再分别处理）
(4) 这类错误统称"数据泄漏"。请再举一个机器学习实验中常见的数据泄漏例子。

**答题要求**：
请在下方答题区作答，建议作答时长 20 分钟。

---

## 第 8 题（编程题·构建简单文本分类模型）

**题目描述**：
某研究小组希望搭建一个最简单的神经网络文本分类器，对新闻标题做 4 分类。
方案为：把每个 token 映射为 64 维词向量，对整句所有 token 的向量取平均，再接一个全连接层输出类别。
已知：词表大小为 5000；词向量维度为 64；类别数为 4。

网络结构：
输入：token 编号序列（形状 [批大小, 句长]）
↓ Embedding: 5000 个词 → 每个 64 维
↓ 对句子内所有 token 向量取平均（得到 [批大小, 64]）
↓ Linear: 64 维 → 4 个类别

为了帮助完成程序，现给出框架：

```python
class TextClassifier(nn.Module):
    def __init__(self):
        super().__init__()
        # 词嵌入层:词表大小 5000,向量维度 64
        self.embedding = nn.Embedding( [①] , [②] )
        # 输出层
        self.fc = nn.Linear( [③] , 4)

    def forward(self, x):
        # x 形状: [batch, seq_len]
        x = self.embedding(x)        # -> [batch, seq_len, 64]
        # 对 seq_len 维取平均,得到整句的表示
        x = [④]                      # -> [batch, 64]
        x = self.fc(x)               # -> [batch, 4]
        return x
```

**请回答以下问题**：
请在下方空白处填入代码：
(1) [①] Embedding 层的第一个参数（词表大小）
(2) [②] Embedding 层的第二个参数（向量维度）
(3) [③] 全连接层的输入维度
(4) [④] 对句长维度取平均的代码（提示：可使用 `x.mean(...)`，注意指定维度)

**答题要求**：
请在下方答题区作答，建议作答时长 25 分钟。
